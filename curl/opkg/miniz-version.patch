--- curl-7.63.0/lib/version.c
+++ curl/lib/version.c
@@ -126,7 +126,11 @@
   }
 
 #ifdef HAVE_LIBZ
+#ifdef MZ_VERSION
+  len = msnprintf(ptr, left, " miniz/%s", zlibVersion());
+#else
   len = msnprintf(ptr, left, " zlib/%s", zlibVersion());
+#endif
   left -= len;
   ptr += len;
 #endif
